# Используем официальный образ с Java
FROM openjdk:17

# Устанавливаем рабочий каталог внутри контейнера
WORKDIR /app

# Копируем собранный JAR-файл в контейнер
COPY ./telegram-0.0.1.jar /app/telegram-0.0.1.jar

# Указываем команду для запуска вашего приложения
ENTRYPOINT ["java", "-jar", "/app/telegram-0.0.1.jar"]

# Опционально: Укажите открытие порта, если ваше приложение использует его
EXPOSE 8080